repos:
  - repo: local
    hooks:
      - id: collect-context
        name: collect-context
        entry: scripts/precommit_run_collect_context.sh
        language: script
        stages: [pre-commit]
        always_run: false
        require_serial: true
        pass_filenames: false
        files: ''
